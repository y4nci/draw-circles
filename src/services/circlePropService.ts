let currentLineWidth = 2;
let currentRadius = 50;

export const getCurrentLineWidth = () => currentLineWidth;

export const setCurrentLineWidth = (width: number) => {
    currentLineWidth = width;
};

export const getCurrentRadius = () => currentRadius;

export const setCurrentRadius = (radius: number) => {
    currentRadius = radius;
};
